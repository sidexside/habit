{
  "name": "habit",
  "scripts": {
    "dev": "node --no-warnings --env-file=.env --experimental-strip-types --watch src/app.ts --trace-warnings"
  },
  "packageManager": "pnpm@10.12.4",
  "dependencies": {
    "fastify": "^5.5.0"
  },
  "devDependencies": {
    "@types/node": "^24.2.1",
    "pino-pretty": "^13.1.1"
  },
  "type": "module",
  "engines": {
    "node": ">=20.6.0"
  }
}
